// 全局变量
var fixedValue = 4;

// 请在本行以下添加你的代码
function incrementer (value) {
  
  return value + 1;
  // 请在本行以上添加你的代码
}

var newValue = incrementer(fixedValue); // 应等于 5
console.log(fixedValue); // 应打印 4